var PasswordBox = Demo.require('./password-box');

var DemoPasswordBox = React.createClass({

	render: function() {
		return (
			<div>
				<PasswordBox value="1234"/>
			</div>
		);
	}
});

React.render(
  React.createElement(DemoPasswordBox, {}), document.getElementById(Demo.domId)
);