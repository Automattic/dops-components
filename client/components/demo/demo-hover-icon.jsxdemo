var HoverIcon = Demo.require( './hover-icon' );

var DemoHoverIcon = React.createClass( {
	
	render: function() {
		return (
			<div>
				<p>hover over me: <HoverIcon name="wordpress">This is a tooltip</HoverIcon></p>
			</div>		
		);
	}
} );

ReactDOM.render(
  React.createElement( DemoHoverIcon, {} ), document.getElementById( Demo.domId )
);